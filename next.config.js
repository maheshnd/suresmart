/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true,
  },
  distDir: "build",
  // output: "export",
  //basePath: "/Sure-Smart",
};

module.exports = nextConfig;
